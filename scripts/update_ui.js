function updateUI() {
  ui.name.innerText = pet.name;
  ui.food.innerText = pet.food;
  ui.fun.innerText = pet.fun;
  ui.sleep.innerText = pet.sleep;

  return console.log("UI Updated Succesfully");
}
